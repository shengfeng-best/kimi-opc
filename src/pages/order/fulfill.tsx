import { View, Text } from '@tarojs/components';
import './fulfill.css';

export default function OrderFulfillPage() {
  return (
    <View className="order-fulfill">
      <Text className="placeholder">交付成果</Text>
    </View>
  );
}
